#include <stdio.h>
#include "main.h"

/**
* _abs - gives the absoulte value of a number.
*
* @r: is the input parameter of function.
*
* Return: 1 if lowercase, 0 if not.
*/

int _abs(int r)
{
	int x;

	x = abs(r);
	return (x);
}
