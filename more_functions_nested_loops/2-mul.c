#include <stdio.h>
#include "main.h"

/**
* mul - Multiplies two numbers.
*
* @a: is the input parameter of function.
* @b: is the input parameter of function.
*
* Return: does the multiplication
*/

int mul(int a, int b)
{
	return (a * b);
}
