#include <stdio.h>
#include "main.h"

/**
* add - adds two numbers.
*
* @x: is the input parameter of function.
* @y: is the input parameter of function.
*
* Return: 1 if lowercase, 0 if not.
*/

int add(int x, int y)
{
	return (x + y);
}
